# Generated by Qodo Gen
from func import main, finder
import unittest

class TestMain(unittest.TestCase):

    # The function correctly counts occurrences of a word in a given text
    def test_correct_word_count(self):
        text = "Hello world! Hello everyone."
        word = "hello"
        expected_count = 2
        self.assertEqual(finder(text, word), expected_count)

    # The function handles empty text input gracefully
    def test_empty_text_input(self):
        text = ""
        word = "hello"
        expected_count = 0
        self.assertEqual(finder(text, word), expected_count)

    # The function manages cases where the word is not present in the text
    def test_word_not_present(self):
        text = "Hello world! Hello everyone."
        word = "goodbye"
        expected_count = 0
        self.assertEqual(finder(text, word), expected_count)